## Rust example on how GPU for Mac mini

Basically it has a main.rs with a boiler plate code to load a shader.wgsl which is loaded and processed by the GPU


## How to compile and run:
```
 cargo build 
 cargo run
```
